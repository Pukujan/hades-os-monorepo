import React from "react";

export function PageCapturePanel() {
  return React.createElement("div", { className: "capture-panel" },
    React.createElement("p", null, "Capture the current page for context."));
}

export default PageCapturePanel;
